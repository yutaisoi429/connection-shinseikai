import React, { useMemo, useState } from 'react';
import { createRoot } from 'react-dom/client';
import {
  AtSign, Bell, BriefcaseBusiness, CalendarDays, Check, ChevronDown, ChevronLeft,
  ChevronRight, CircleCheck, Clock3, Download, FileText, Filter, FolderOpen,
  LayoutDashboard, ListTodo, Mail, MoreHorizontal, Plus, Search, Send, Settings,
  SlidersHorizontal, Upload, UserPlus, Users, X
} from 'lucide-react';
import './styles.css';

const initialProjects = [
  { id: 1, name: '令和6年度 総会準備', owner: '髙橋 宗一郎', council: '真誓会 事務局', status: '進行中', progress: 68, due: '2026-10-28', createdAt: '2026-08-02', members: ['髙', '佐', '田'] },
  { id: 2, name: '地域交流会 企画・運営', owner: '田中 誠', council: '地域連携評議会', status: '進行中', progress: 42, due: '2026-11-12', createdAt: '2026-09-01', members: ['田', '山', '鈴'] },
  { id: 3, name: '会報誌「真」秋号制作', owner: '佐藤 美咲', council: '広報評議会', status: '確認待ち', progress: 84, due: '2026-10-21', createdAt: '2026-07-18', members: ['佐', '伊'] },
  { id: 4, name: '新規会員オリエンテーション', owner: '髙橋 宗一郎', council: '組織運営評議会', status: '企画中', progress: 18, due: '2026-11-30', createdAt: '2026-09-12', members: ['髙', '伊'] },
];

const initialTasks = [
  { id: 1, projectId: 1, title: '総会資料の最終確認', assignee: '髙橋 宗一郎', avatar: '髙', dueDate: '2026-09-24', createdAt: '2026-09-14', comments: 3, stale: false, done: false },
  { id: 2, projectId: 2, title: '会場レイアウト案の提出', assignee: '田中 誠', avatar: '田', dueDate: '2026-09-28', createdAt: '2026-09-10', comments: 1, stale: true, done: false },
  { id: 3, projectId: 3, title: '原稿内容の校正・承認', assignee: '佐藤 美咲', avatar: '佐', dueDate: '2026-10-03', createdAt: '2026-09-02', comments: 5, stale: false, done: false },
  { id: 4, projectId: 2, title: '参加者リストの更新', assignee: '山本 健一', avatar: '山', dueDate: '2026-10-08', createdAt: '2026-08-25', comments: 0, stale: false, done: false },
  { id: 5, projectId: 1, title: '出欠確認メールの送信', assignee: '佐藤 美咲', avatar: '佐', dueDate: '2026-09-20', createdAt: '2026-08-17', comments: 2, stale: false, done: true },
];

const projectFiles = [
  { id: 1, projectId: 1, name: '総会資料_最終版.pdf', type: 'PDF', size: '3.2 MB', owner: '佐藤 美咲', updatedAt: '2026-09-24' },
  { id: 2, projectId: 2, name: '会場レイアウト案_v2.pptx', type: 'PowerPoint', size: '8.7 MB', owner: '田中 誠', updatedAt: '2026-09-22' },
  { id: 3, projectId: 3, name: '会報誌_初校データ.pdf', type: 'PDF', size: '12.4 MB', owner: '佐藤 美咲', updatedAt: '2026-09-17' },
  { id: 4, projectId: 2, name: '参加者リスト.xlsx', type: 'Excel', size: '124 KB', owner: '山本 健一', updatedAt: '2026-09-16' },
  { id: 5, projectId: 1, name: '会場案内図.png', type: '画像', size: '940 KB', owner: '髙橋 宗一郎', updatedAt: '2026-09-12' },
];

const initialMembers = [
  { id: 1, name: '髙橋 宗一郎', email: 'takahashi@shinseikai.jp', company: '株式会社髙橋商事', council: '組織運営評議会', role: '事務局長', status: '参加中', avatar: '髙' },
  { id: 2, name: '佐藤 美咲', email: 'sato@example.jp', company: '佐藤企画株式会社', council: '広報評議会', role: '評議員', status: '参加中', avatar: '佐' },
  { id: 3, name: '田中 誠', email: 'tanaka@example.jp', company: '株式会社地域デザイン', council: '地域連携評議会', role: '委員長', status: '参加中', avatar: '田' },
  { id: 4, name: '山本 健一', email: 'yamamoto@example.jp', company: '山本経営事務所', council: '地域連携評議会', role: '評議員', status: '参加中', avatar: '山' },
  { id: 5, name: '招待中', email: 'ito@example.jp', company: '—', council: '未設定', role: '未設定', status: '招待中', avatar: '伊' },
];

const notifications = [
  { id: 1, type: 'mention', unread: true, title: '佐藤 美咲さんがあなたをメンションしました', detail: '「@髙橋さん 最終版をご確認いただけますか？」', project: '会報誌「真」秋号制作', time: '10分前' },
  { id: 2, type: 'stale', unread: true, title: '72時間コメントがありません', detail: '会場レイアウト案の提出', project: '地域交流会 企画・運営', time: '1時間前' },
  { id: 3, type: 'task', unread: true, title: '新しいタスクが割り当てられました', detail: '総会資料の最終確認', project: '令和6年度 総会準備', time: '3時間前' },
];

const nav = [[LayoutDashboard, 'ダッシュボード'], [BriefcaseBusiness, '案件'], [ListTodo, 'タスク'], [Bell, '通知'], [Users, 'メンバー']];
const fmt = value => new Intl.DateTimeFormat('ja-JP', { month: 'numeric', day: 'numeric' }).format(new Date(value));
function projectFor(task, projects) { return projects.find(p => p.id === task.projectId); }
function BrandMark() { return <img className="brand-mark compact" src={`${import.meta.env.BASE_URL}shinseikai-mark.svg`} alt="真誓会ロゴ"/>; }
function Avatar({ label, small=false }) { return <span className={`avatar ${small?'small':''}`}>{label}</span>; }

function Sidebar({ active, setActive, currentUser, openProfile }) {
  return <aside className="sidebar"><button className="brand" onClick={()=>setActive('ダッシュボード')}><BrandMark/><div><strong>真誓会</strong><span>SHINSEIKAI CONNECT</span></div></button><nav><p className="nav-label">MENU</p>{nav.map(([Icon,label])=><button key={label} className={active===label?'active':''} onClick={()=>setActive(label)}><Icon size={19}/><span>{label}</span>{label==='通知'&&<em>3</em>}</button>)}</nav><div className="side-footer"><button className="profile profile-button" onClick={openProfile}><Avatar label={currentUser.avatar}/><div><strong>{currentUser.name}</strong><span>{currentUser.role}</span></div><ChevronRight size={16}/></button></div></aside>;
}
function Header({ query,setQuery,currentUser }) { return <header><div className="search"><Search size={18}/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="案件名・タスク名を部分一致で検索"/>{query&&<button onClick={()=>setQuery('')}><X size={14}/></button>}<kbd>⌘ K</kbd></div><div className="header-right"><button className="icon-button"><Bell size={20}/><i/></button><div className="header-divider"/><Avatar label={currentUser.avatar} small/><div className="header-user"><strong>{currentUser.name}</strong><span>{currentUser.role}</span></div><ChevronDown size={15}/></div></header>; }
function PageTitle({ eyebrow,title,description,action }) { return <div className="page-title"><div><p>{eyebrow}</p><h1>{title}</h1><span>{description}</span></div>{action}</div>; }
function SectionHead({ eyebrow,title,action }) { return <div className="panel-head"><div><p>{eyebrow}</p><h2>{title}</h2></div>{action}</div>; }
function Stat({ icon:Icon,tone,value,label,detail }) { return <div className="stat card"><div className={`stat-icon ${tone}`}><Icon size={21}/></div><div className="stat-copy"><span>{label}</span><strong>{value}</strong><small>{detail}</small></div></div>; }
function Empty({ query }) { return <div className="empty-state"><Search/><strong>該当する項目がありません</strong><span>「{query}」に一致するデータは見つかりませんでした。</span></div>; }

function TaskRow({ task,projects,toggle,open }) { const project=projectFor(task,projects); return <div className={`task-row ${task.done?'completed':''}`} onClick={()=>open(task)}><button className="check" aria-label="完了状態を切り替え" onClick={e=>{e.stopPropagation();toggle(task.id)}}>{task.done&&<Check size={14}/>}</button><div className="task-main"><strong>{task.title}</strong><span><BriefcaseBusiness size={13}/>{project?.name}</span></div><div className="person"><Avatar label={task.avatar} small/><span>{task.assignee}</span></div><div className="date"><CalendarDays size={15}/>{fmt(task.dueDate)}</div><div className="comment"><AtSign size={15}/>{task.comments}</div>{task.stale?<span className="stale"><Clock3 size={13}/>72h</span>:<span/>}<ChevronRight size={16}/></div>; }

function ProjectCard({ project,tasks,onOpen,onStatus }) { const counts=tasks.filter(t=>t.projectId===project.id); const completed=counts.filter(t=>t.done).length; const progress=counts.length?Math.round(completed/counts.length*100):0; return <article className="project-card" onClick={()=>onOpen(project)}><div className="project-top"><span className="project-icon"><FolderOpen size={20}/></span><select className={`status-select status-${project.status}`} value={project.status} onClick={e=>e.stopPropagation()} onChange={e=>onStatus(project.id,e.target.value)}><option>企画中</option><option>進行中</option><option>確認待ち</option><option>完了</option></select><ChevronRight size={18}/></div><h3>{project.name}</h3><p>{project.council} ・ 担当 {project.owner}</p><div className="progress-title"><span>タスク進捗</span><strong>{progress}%</strong></div><div className="progress"><i style={{width:`${progress}%`}}/></div><footer><div className="avatars">{project.members.map((m,i)=><Avatar label={m} small key={i}/>)}</div><span><ListTodo size={14}/>{completed} / {counts.length}</span><span><CalendarDays size={14}/>{fmt(project.due)}</span></footer></article>; }
function ProjectGrid(props) { return <div className="project-grid">{props.items.map(p=><ProjectCard key={p.id} project={p} {...props}/>)}</div>; }

function Dashboard({ query,projects,tasks,toggle,openTask,openProject,setActive,onStatus }) {
  const q=query.trim().toLowerCase(); const ps=projects.filter(p=>`${p.name}${p.owner}${p.council}`.toLowerCase().includes(q)); const ts=tasks.filter(t=>`${t.title}${t.assignee}${projectFor(t,projects)?.name}`.toLowerCase().includes(q));
  return <><PageTitle eyebrow="OVERVIEW" title="おはようございます、髙橋さん" description={q?`「${query}」の検索結果`:'今日も、つながりを大切に。'} action={<button className="primary-button" onClick={()=>setActive('タスク')}><Plus size={18}/>タスクを登録</button>}/><section className="stats-grid"><Stat icon={ListTodo} tone="blue" label="自分の未完了タスク" value={tasks.filter(t=>!t.done).length} detail="今週の期限 3件"/><Stat icon={Clock3} tone="red" label="72時間未返信" value="2" detail="確認が必要です"/><Stat icon={BriefcaseBusiness} tone="gold" label="進行中の案件" value={projects.filter(p=>p.status==='進行中').length} detail="案件を確認"/><Stat icon={Bell} tone="navy" label="未読の通知" value="3" detail="メンション 1件"/></section><section className="content-grid"><div className="card panel"><SectionHead eyebrow="TASKS" title={q?'該当するタスク':'対応が必要なタスク'} action={<button className="text-button" onClick={()=>setActive('タスク')}>すべて見る <ChevronRight size={16}/></button>}/>{ts.length?<div className="task-list">{ts.slice(0,4).map(t=><TaskRow key={t.id} task={t} projects={projects} toggle={toggle} open={openTask}/>)}</div>:<Empty query={query}/>}</div><div className="card panel notification-preview"><SectionHead eyebrow="NOTIFICATIONS" title="最新の通知"/>{notifications.map(n=><NotificationItem key={n.id} item={n}/>)}</div></section><div className="card panel"><SectionHead eyebrow="PROJECTS" title={q?'該当する案件':'進行中の案件'} action={<button className="text-button" onClick={()=>setActive('案件')}>案件一覧へ <ChevronRight size={16}/></button>}/>{ps.length?<ProjectGrid items={ps.slice(0,3)} tasks={tasks} onOpen={openProject} onStatus={onStatus}/>:<Empty query={query}/>}</div></>;
}

function FilterPanel({ owners,projectFilter,setProjectFilter,close }) { const toggleOwner=name=>setProjectFilter({...projectFilter,owners:projectFilter.owners.includes(name)?projectFilter.owners.filter(x=>x!==name):[...projectFilter.owners,name]}); return <div className="filter-popover"><div className="filter-title"><strong>案件を絞り込む</strong><button onClick={close}><X size={16}/></button></div><label>作成時期<select value={projectFilter.created} onChange={e=>setProjectFilter({...projectFilter,created:e.target.value})}><option value="">すべて</option><option value="recent">2026年9月以降</option><option value="older">2026年8月以前</option></select></label><label>ステータス<select value={projectFilter.status} onChange={e=>setProjectFilter({...projectFilter,status:e.target.value})}><option value="">すべて</option><option>企画中</option><option>進行中</option><option>確認待ち</option><option>完了</option></select></label><fieldset><legend>担当者</legend>{owners.map(o=><label className="checkbox" key={o}><input type="checkbox" checked={projectFilter.owners.includes(o)} onChange={()=>toggleOwner(o)}/>{o}</label>)}</fieldset><button className="clear-filter" onClick={()=>setProjectFilter({status:'',owners:[],created:''})}>条件をクリア</button></div>; }
function ProjectsPage({ query,projects,tasks,openProject,onStatus }) { const [sort,setSort]=useState('new'); const [showFilter,setShowFilter]=useState(false); const [filter,setFilter]=useState({status:'',owners:[],created:''}); const owners=[...new Set(projects.map(p=>p.owner))]; const items=useMemo(()=>projects.filter(p=>`${p.name}${p.owner}${p.council}`.toLowerCase().includes(query.toLowerCase())).filter(p=>!filter.created||(filter.created==='recent'?p.createdAt>='2026-09-01':p.createdAt<'2026-09-01')).filter(p=>!filter.status||p.status===filter.status).filter(p=>!filter.owners.length||filter.owners.includes(p.owner)).sort((a,b)=>sort==='new'?b.createdAt.localeCompare(a.createdAt):a.createdAt.localeCompare(b.createdAt)),[projects,query,filter,sort]); return <><PageTitle eyebrow="PROJECTS" title="案件" description="案件を開くと、関連するタスクとファイルをまとめて確認できます。" action={<button className="primary-button"><Plus size={18}/>新しい案件</button>}/><div className="toolbar"><span className="result-count">{items.length}件の案件</span><div className="toolbar-actions"><label className="sort-control">並べ替え<select value={sort} onChange={e=>setSort(e.target.value)}><option value="new">作成日：新しい順</option><option value="old">作成日：古い順</option></select></label><div className="filter-anchor"><button className={`outline-button ${filter.status||filter.created||filter.owners.length?'filtered':''}`} onClick={()=>setShowFilter(!showFilter)}><Filter size={15}/>絞り込み{(filter.status||filter.created||filter.owners.length>0)&&<b>{(filter.status?1:0)+(filter.created?1:0)+filter.owners.length}</b>}</button>{showFilter&&<FilterPanel owners={owners} projectFilter={filter} setProjectFilter={setFilter} close={()=>setShowFilter(false)}/>}</div></div></div><div className="card panel">{items.length?<ProjectGrid items={items} tasks={tasks} onOpen={openProject} onStatus={onStatus}/>:<Empty query={query}/>}</div></>; }

function TasksPage({ query,projects,tasks,toggle,openTask,openNew }) { const [view,setView]=useState('open'); const [sort,setSort]=useState('new'); const [from,setFrom]=useState(''); const [to,setTo]=useState(''); const items=useMemo(()=>tasks.filter(t=>view==='done'?t.done:view==='mine'?!t.done&&t.assignee==='髙橋 宗一郎':view==='stale'?!t.done&&t.stale:!t.done).filter(t=>`${t.title}${t.assignee}${projectFor(t,projects)?.name}`.toLowerCase().includes(query.toLowerCase())).filter(t=>(!from||t.dueDate>=from)&&(!to||t.dueDate<=to)).sort((a,b)=>sort==='new'?b.createdAt.localeCompare(a.createdAt):a.createdAt.localeCompare(b.createdAt)),[tasks,view,sort,from,to,query,projects]); return <><PageTitle eyebrow="TASK MANAGEMENT" title="タスク" description="チェックを付けたタスクは完了済みに移動します。" action={<button className="primary-button" onClick={openNew}><Plus size={18}/>新しいタスク</button>}/><div className="task-filter-row"><div className="tabs task-view-tabs"><button className={view==='open'?'selected':''} onClick={()=>setView('open')}>未完了 <b>{tasks.filter(t=>!t.done).length}</b></button><button className={view==='done'?'selected':''} onClick={()=>setView('done')}>完了済み <b>{tasks.filter(t=>t.done).length}</b></button><button className={view==='mine'?'selected':''} onClick={()=>setView('mine')}>自分宛のタスク <b>{tasks.filter(t=>!t.done&&t.assignee==='髙橋 宗一郎').length}</b></button><button className={view==='stale'?'selected warning':''} onClick={()=>setView('stale')}><Clock3 size={14}/>72時間オーバー <b>{tasks.filter(t=>!t.done&&t.stale).length}</b></button></div></div><div className="toolbar task-toolbar"><span className="result-count">{items.length}件を表示</span><div className="toolbar-actions"><label className="date-control">期限<input type="date" value={from} onChange={e=>setFrom(e.target.value)}/><span>〜</span><input type="date" value={to} onChange={e=>setTo(e.target.value)}/></label><label className="sort-control">並べ替え<select value={sort} onChange={e=>setSort(e.target.value)}><option value="new">作成日：新しい順</option><option value="old">作成日：古い順</option></select></label></div></div><div className="card panel task-page"><div className="table-heading"><span>タスク / 案件</span><span>担当者</span><span>期限</span><span>コメント</span><span>状態</span></div>{items.length?items.map(t=><TaskRow key={t.id} task={t} projects={projects} toggle={toggle} open={openTask}/>):<Empty query={query||'指定した条件'}/>}</div></>; }

function ProjectDetail({ project,projects,tasks,files,toggle,openTask,openNew,back,onStatus }) { const [tab,setTab]=useState('tasks'); const [sort,setSort]=useState('new'); const [type,setType]=useState(''); const [owner,setOwner]=useState(''); const projectTasks=tasks.filter(t=>t.projectId===project.id).sort((a,b)=>sort==='new'?b.createdAt.localeCompare(a.createdAt):a.createdAt.localeCompare(b.createdAt)); const owners=[...new Set(files.filter(f=>f.projectId===project.id).map(f=>f.owner))]; const visibleFiles=files.filter(f=>f.projectId===project.id).filter(f=>!type||f.type===type).filter(f=>!owner||f.owner===owner).sort((a,b)=>sort==='new'?b.updatedAt.localeCompare(a.updatedAt):a.updatedAt.localeCompare(b.updatedAt)); return <><button className="back-button" onClick={back}><ChevronLeft size={17}/>案件一覧へ戻る</button><div className="project-detail-head"><div><span className="project-icon large"><FolderOpen size={25}/></span><div><p>{project.council}</p><h1>{project.name}</h1><span>担当：{project.owner} ・ 作成日：{fmt(project.createdAt)}</span></div></div><select className={`detail-status status-${project.status}`} value={project.status} onChange={e=>onStatus(project.id,e.target.value)}><option>企画中</option><option>進行中</option><option>確認待ち</option><option>完了</option></select></div><div className="detail-tabs"><button className={tab==='tasks'?'active':''} onClick={()=>setTab('tasks')}><ListTodo/>タスク <b>{projectTasks.length}</b></button><button className={tab==='files'?'active':''} onClick={()=>setTab('files')}><FileText/>ファイル <b>{files.filter(f=>f.projectId===project.id).length}</b></button></div>{tab==='tasks'?<><div className="toolbar"><span className="result-count">完了 {projectTasks.filter(t=>t.done).length} / {projectTasks.length}</span><div className="toolbar-actions"><label className="sort-control">並べ替え<select value={sort} onChange={e=>setSort(e.target.value)}><option value="new">作成日：新しい順</option><option value="old">作成日：古い順</option></select></label><button className="primary-button" onClick={openNew}><Plus size={16}/>タスクを追加</button></div></div><div className="card panel task-page">{projectTasks.map(t=><TaskRow key={t.id} task={t} projects={projects} toggle={toggle} open={openTask}/>)}</div></>:<><div className="toolbar"><span className="result-count">{visibleFiles.length}件のファイル</span><div className="toolbar-actions"><label className="sort-control">種類<select value={type} onChange={e=>setType(e.target.value)}><option value="">すべて</option><option>PDF</option><option>PowerPoint</option><option>Excel</option><option>画像</option></select></label><label className="sort-control">追加者<select value={owner} onChange={e=>setOwner(e.target.value)}><option value="">すべて</option>{owners.map(o=><option key={o}>{o}</option>)}</select></label><label className="sort-control">並べ替え<select value={sort} onChange={e=>setSort(e.target.value)}><option value="new">更新日：新しい順</option><option value="old">更新日：古い順</option></select></label><button className="primary-button"><Upload size={16}/>追加</button></div></div><FileTable files={visibleFiles}/></>}</>; }
function FileTable({ files }) { return <div className="card panel data-card"><div className="table-heading files-head"><span>ファイル名</span><span>種類</span><span>追加者</span><span>更新日</span><span/></div>{files.map(f=><div className="file-row" key={f.id}><span className="file-name"><i><FileText size={20}/></i><span><strong>{f.name}</strong><small>{f.size}</small></span></span><span>{f.type}</span><span>{f.owner}</span><span>{fmt(f.updatedAt)}</span><button><Download size={17}/></button></div>)}</div>; }

function NotificationItem({item}) { const Icon=item.type==='mention'?AtSign:item.type==='stale'?Clock3:ListTodo; return <div className={`notification-item ${item.unread?'unread':''}`}><span className={`notice-icon ${item.type}`}><Icon size={17}/></span><div><strong>{item.title}</strong><p>{item.detail}</p><small>{item.project} ・ {item.time}</small></div>{item.unread&&<i/>}</div>; }
function Toggle({checked,onChange,label,description}) { return <label className="setting-toggle"><span><strong>{label}</strong>{description&&<small>{description}</small>}</span><input type="checkbox" checked={checked} onChange={e=>onChange(e.target.checked)}/><i/></label>; }
function NotificationsPage({onSaved}) { const [section,setSection]=useState('list'); const [channels,setChannels]=useState({system:true,email:true}); const [events,setEvents]=useState({taskCreated:true,taskAssigned:true,taskCompleted:false,comment:true,mention:true,fileAdded:true,fileUpdated:false,projectStatus:true,deadline:true,stale:true,memberInvite:false}); const setEvent=(key,value)=>setEvents({...events,[key]:value}); const eventOptions=[['taskCreated','タスクが追加されたとき','自分が関係する案件にタスクが追加された場合'],['taskAssigned','タスクが割り当てられたとき','自分が担当者に指定された場合'],['taskCompleted','タスクが完了したとき','担当タスクが完了になった場合'],['comment','タスクにコメントが追加されたとき','関係するタスクにコメントが投稿された場合'],['mention','メンションされたとき','コメントで自分が @メンションされた場合'],['fileAdded','資料が追加されたとき','関係する案件へファイルが追加された場合'],['fileUpdated','資料が更新されたとき','既存ファイルが差し替えられた場合'],['projectStatus','案件のステータスが変わったとき','企画中・進行中・確認待ち・完了が変更された場合'],['deadline','期限が近づいたとき','担当タスクの期限前日になった場合'],['stale','72時間を超えたとき','タスクに確認・コメントがない状態が72時間続いた場合'],['memberInvite','メンバーの招待・参加時','新しいメンバーが招待または参加した場合']]; return <><PageTitle eyebrow="NOTIFICATIONS" title="通知" description="通知の確認と、受け取り方法・対象イベントを設定できます。" action={section==='list'?<button className="outline-button"><Check size={16}/>すべて既読にする</button>:<button className="primary-button" onClick={onSaved}><Check size={16}/>設定を保存</button>}/><div className="notification-tabs"><button className={section==='list'?'active':''} onClick={()=>setSection('list')}>通知一覧 <b>3</b></button><button className={section==='settings'?'active':''} onClick={()=>setSection('settings')}><SlidersHorizontal size={16}/>通知設定</button></div>{section==='list'?<div className="content-narrow card panel">{notifications.map(n=><NotificationItem key={n.id} item={n}/>)}</div>:<div className="notification-settings"><section className="card settings-card"><div className="settings-heading"><span className="settings-icon"><Mail size={19}/></span><div><h2>通知先</h2><p>通知を受け取る方法を選択してください。</p></div></div><Toggle label="システム内通知" description="画面内の通知一覧と通知バッジに表示します" checked={channels.system} onChange={v=>setChannels({...channels,system:v})}/><Toggle label="メール通知" description="登録メールアドレス takahashi@shinseikai.jp へ送信します" checked={channels.email} onChange={v=>setChannels({...channels,email:v})}/></section><section className="card settings-card"><div className="settings-heading"><span className="settings-icon"><Bell size={19}/></span><div><h2>通知するイベント</h2><p>通知を受け取りたい操作・状態を選択してください。</p></div></div><div className="event-settings">{eventOptions.map(([key,label,description])=><Toggle key={key} label={label} description={description} checked={events[key]} onChange={v=>setEvent(key,v)}/>)}</div></section></div>}</>; }
function MembersPage({members,openInvite,query}) { const items=members.filter(m=>`${m.name}${m.email}${m.company}${m.council}${m.role}`.toLowerCase().includes(query.toLowerCase())); return <><PageTitle eyebrow="MEMBERS" title="メンバー" description="協議会に参加するメンバーと所属情報を管理します。" action={<button className="primary-button" onClick={openInvite}><UserPlus size={18}/>メールで招待</button>}/><div className="card panel data-card"><div className="table-heading member-head"><span>メンバー</span><span>会社名</span><span>所属評議会</span><span>役職</span><span>状態</span><span/></div>{items.map(m=><div className="member-row" key={m.id}><span className="member-name"><Avatar label={m.avatar}/><span><strong>{m.name}</strong><small>{m.email}</small></span></span><span>{m.company}</span><span>{m.council}</span><span>{m.role}</span><span className={`member-status ${m.status==='招待中'?'pending':''}`}>{m.status}</span><MoreHorizontal size={18}/></div>)}</div></>; }

function TaskModal({task,projects,close}) { const [comments,setComments]=useState([{name:'佐藤 美咲',avatar:'佐',text:'資料の修正版をアップロードしました。@髙橋 宗一郎 さん、ご確認をお願いいたします。',time:'10分前'}]); const [text,setText]=useState(''); const send=()=>{if(!text.trim())return;setComments([...comments,{name:'髙橋 宗一郎',avatar:'髙',text,time:'たった今'}]);setText('')}; return <div className="modal-backdrop"><div className="drawer"><div className="drawer-head"><div><span>TASK DETAIL</span><h2>{task.title}</h2></div><button onClick={close}><X/></button></div><div className="task-meta"><div><small>案件</small><strong>{projectFor(task,projects)?.name}</strong></div><div><small>担当者</small><strong><Avatar label={task.avatar} small/>{task.assignee}</strong></div><div><small>期限</small><strong>{fmt(task.dueDate)}</strong></div></div><div className="comment-title"><h3>コメント</h3><span>{comments.length}件</span></div><div className="comments">{comments.map((c,i)=><div className="comment-card" key={i}><Avatar label={c.avatar} small/><div><strong>{c.name}<small>{c.time}</small></strong><p>{c.text}</p></div></div>)}</div><div className="comment-box"><textarea value={text} onChange={e=>setText(e.target.value)} placeholder="コメントを入力… @ でメンバーをメンション"/><div><button className="mention-button" onClick={()=>setText(text+'@佐藤 美咲 ')}><AtSign size={16}/>メンション</button><button className="send-button" onClick={send}><Send size={15}/>送信</button></div></div></div></div>; }
function ProfileModal({profile,close,onSave}) { const [form,setForm]=useState(profile); const change=e=>setForm({...form,[e.target.name]:e.target.value}); return <div className="modal-backdrop"><div className="modal"><div className="modal-head"><div><span>MY PROFILE</span><h2>プロフィールを変更</h2><p>所属情報と連絡先を更新できます。</p></div><button onClick={close}><X/></button></div><div className="profile-edit-head"><Avatar label={(form.name||'髙')[0]}/><div><strong>{form.name}</strong><span>{form.email}</span></div></div><div className="form-grid"><label>氏名<input name="name" value={form.name} onChange={change}/></label><label>メールアドレス<input name="email" type="email" value={form.email} onChange={change}/></label><label>会社名<input name="company" value={form.company} onChange={change}/></label><label>所属評議会<input name="council" value={form.council} onChange={change}/></label><label>役職<input name="role" value={form.role} onChange={change}/></label></div><div className="modal-actions"><button onClick={close}>キャンセル</button><button className="primary-button" onClick={()=>onSave(form)}>変更を保存</button></div></div></div>; }
function FormModal({type,close,onInvite,onCreateTask,projects,members,defaultProjectId}) { const [form,setForm]=useState({email:'',name:'',company:'',council:'地域連携評議会',role:'評議員',title:'',projectId:String(defaultProjectId||projects[0]?.id||''),dueDate:'',assignees:[]}); const change=e=>setForm({...form,[e.target.name]:e.target.value}); const toggleAssignee=name=>setForm({...form,assignees:form.assignees.includes(name)?form.assignees.filter(x=>x!==name):[...form.assignees,name]}); const submit=()=>type==='invite'?onInvite(form):onCreateTask(form); return <div className="modal-backdrop"><div className="modal"><div className="modal-head"><div><span>{type==='invite'?'INVITE MEMBER':'NEW TASK'}</span><h2>{type==='invite'?'メンバーをメールで招待':'新しいタスク'}</h2></div><button onClick={close}><X/></button></div>{type==='invite'?<div className="form-grid"><label className="full">メールアドレス<input name="email" value={form.email} onChange={change} type="email" placeholder="member@example.jp"/></label><label>氏名<input name="name" value={form.name} onChange={change}/></label><label>会社名<input name="company" value={form.company} onChange={change}/></label><label>所属評議会<select name="council" value={form.council} onChange={change}><option>地域連携評議会</option><option>広報評議会</option><option>組織運営評議会</option></select></label><label>役職<input name="role" value={form.role} onChange={change}/></label></div>:<><label>タスク名<input name="title" value={form.title} onChange={change} placeholder="対応内容を入力"/></label><div className="form-grid"><label>案件<select name="projectId" value={form.projectId} onChange={change}>{projects.map(p=><option key={p.id} value={p.id}>{p.name}</option>)}</select></label><label>期限<input name="dueDate" value={form.dueDate} onChange={change} type="date"/></label></div><fieldset className="assignee-picker"><legend>担当メンバー（複数選択可）</legend>{members.filter(m=>m.status==='参加中').map(m=><label key={m.id} className={form.assignees.includes(m.name)?'selected':''}><input type="checkbox" checked={form.assignees.includes(m.name)} onChange={()=>toggleAssignee(m.name)}/><Avatar label={m.avatar} small/><span><strong>{m.name}</strong><small>{m.company}</small></span></label>)}</fieldset></>}<div className="modal-actions"><button onClick={close}>キャンセル</button><button className="primary-button" disabled={type==='invite'?!form.email:!form.title||!form.assignees.length} onClick={submit}>{type==='invite'?'招待メールを送信':`タスクを作成（${form.assignees.length}名）`}</button></div></div></div>; }

function App() {
  const [active,setActiveRaw]=useState('ダッシュボード'); const [currentUser,setCurrentUser]=useState(initialMembers[0]); const [query,setQuery]=useState(''); const [projects,setProjects]=useState(initialProjects); const [tasks,setTasks]=useState(initialTasks); const [members,setMembers]=useState(initialMembers); const [selectedProject,setSelectedProject]=useState(null); const [selectedTask,setSelectedTask]=useState(null); const [modal,setModal]=useState(null); const [toast,setToast]=useState('');
  const setActive=value=>{setActiveRaw(value);setSelectedProject(null)}; const toggle=id=>setTasks(tasks.map(t=>t.id===id?{...t,done:!t.done}:t)); const updateStatus=(id,status)=>setProjects(projects.map(p=>p.id===id?{...p,status}:p));
  const createTask=form=>{const created=form.assignees.map((name,index)=>{const member=members.find(m=>m.name===name);return {id:Date.now()+index,projectId:Number(form.projectId),title:form.title,assignee:name,avatar:member?.avatar||name[0],dueDate:form.dueDate||new Date().toISOString().slice(0,10),createdAt:new Date().toISOString().slice(0,10),comments:0,stale:false,done:false}});setTasks([...tasks,...created]);setModal(null);setToast(`${created.length}件のタスクを作成しました`);setTimeout(()=>setToast(''),3000)};
  const saveProfile=form=>{setCurrentUser({...form,avatar:(form.name||'髙')[0]});setMembers(members.map(m=>m.id===1?{...m,...form,avatar:(form.name||'髙')[0]}:m));setModal(null);setToast('プロフィールを更新しました');setTimeout(()=>setToast(''),3000)};
  const notifySaved=()=>{setToast('通知設定を保存しました');setTimeout(()=>setToast(''),3000)};
  const invite=form=>{setMembers([...members,{id:Date.now(),name:form.name||'招待中',email:form.email,company:form.company||'—',council:form.council,role:form.role,status:'招待中',avatar:(form.name||form.email)[0]}]);setModal(null);setToast(`${form.email} に招待を送信しました`);setTimeout(()=>setToast(''),3000)};
  let page;
  if(selectedProject) page=<ProjectDetail project={projects.find(p=>p.id===selectedProject.id)} projects={projects} tasks={tasks} files={projectFiles} toggle={toggle} openTask={setSelectedTask} openNew={()=>setModal({type:'task',projectId:selectedProject.id})} back={()=>setSelectedProject(null)} onStatus={updateStatus}/>;
  else if(active==='案件') page=<ProjectsPage query={query} projects={projects} tasks={tasks} openProject={setSelectedProject} onStatus={updateStatus}/>;
  else if(active==='タスク') page=<TasksPage query={query} projects={projects} tasks={tasks} toggle={toggle} openTask={setSelectedTask} openNew={()=>setModal({type:'task'})}/>;
  else if(active==='通知') page=<NotificationsPage onSaved={notifySaved}/>;
  else if(active==='メンバー') page=<MembersPage members={members} query={query} openInvite={()=>setModal({type:'invite'})}/>;
  else page=<Dashboard query={query} projects={projects} tasks={tasks} toggle={toggle} openTask={setSelectedTask} openProject={setSelectedProject} setActive={setActive} onStatus={updateStatus}/>;
  return <div className="app"><Sidebar active={active} setActive={setActive} currentUser={currentUser} openProfile={()=>setModal({type:'profile'})}/><div className="shell"><Header query={query} setQuery={setQuery} currentUser={currentUser}/><main>{page}</main><footer className="page-footer"><span>© 真誓会</span><span>人と人、心と心をつなぐ。</span></footer></div>{selectedTask&&<TaskModal task={selectedTask} projects={projects} close={()=>setSelectedTask(null)}/>} {modal&&modal.type==='profile'?<ProfileModal profile={currentUser} close={()=>setModal(null)} onSave={saveProfile}/>:modal&&<FormModal type={modal.type} defaultProjectId={modal.projectId} projects={projects} members={members} close={()=>setModal(null)} onInvite={invite} onCreateTask={createTask}/>} {toast&&<div className="toast"><CircleCheck size={18}/>{toast}</div>}</div>;
}
createRoot(document.getElementById('root')).render(<App/>);
